#APP_ABI := all
APP_ABI := armeabi-v7a
APP_PLATFORM := android-14
#APP_PROJECT_PATH := $(NDK_PROJECT_PATH)
#APP_BUILD_SCRIPT := $(APP_PROJECT_PATH)/Android.mk
#NDK_APP_DST_DIR  := $(APP_PROJECT_PATH)/../android_proj/libs/$(TARGET_ARCH_ABI)
APP_MODULES := freetype2-static libonig libpng
